import React from 'react';

let Visualizer = (props) => {
    return(
        <div className='visualizer-container' style={{marginRight: '50px'}}>
            array bars here
        </div>
    );
}

export default Visualizer;